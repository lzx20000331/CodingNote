---
outline: deep
---

# declare 关键字

declare 关键字用于在 TypeScript 中声明变量、函数、类、接口等，而不进行实际的实现。它通常用于在 TypeScript 中引用外部的 JavaScript 代码或库。

# .d.ts 类型声明文件

.d.ts 文件是 TypeScript 中的类型声明文件，用于描述 JavaScript 代码的类型信息。它通常用于在 TypeScript 中引用外部的 JavaScript 代码或库，而不需要实际的实现。
